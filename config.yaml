github:
  repo:
    owner: "Axpz"
    name: "store"
    branch: "main"
    tables:
      path: "tables"
      users: "users.json"
      orders: "orders.json"
      products: "products.json"
      comments: "comments.json"
server:
  port: 8080
  host: "localhost"

storage:
  type: "github" # 可选值: "github" 或 "local"
  path: "tables" # 仅当 type 为 "local" 时使用 